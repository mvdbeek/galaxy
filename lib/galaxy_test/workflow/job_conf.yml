runners:
  local:
    load: galaxy.jobs.runners.local:LocalJobRunner
    workers: 1

execution:
  default: local_docker
  environments:
    local_docker:
      runner: local
      docker_enabled: true
    local:
      runner: local

tools:
  - class: local
    environment: local
